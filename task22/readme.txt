ƒл€ решени€ поставленной задачи воспользуемс€ следущими классами: Main, Vector и VectorOperation. IVector €вл€етс€ интерфейсом.
 ласс Vector сделан дл€ создани€ динамических массивов, в этом классе определено несколько метадов:

1) get(int index) - получает на вход целое число и возвращает по нему соответствующий элемент массива,
  
2) size() - возвращает размер массива,

3) add(double value) - обавл€ет в имеющийс€ массив новый элемент value.



— помощью класса VectorOperation можно

1) найти сумму/разность  двух векторов ( sum(Vector vector1, Vector vector2)/div(Vector vector1, Vector vector2) ), 

2) увеличить вектор на константу ( incr(Vector vector1, double k) ),

3) получить норму вектора ( nor(Vector vector1) ),

4) узнать значение скал€рного произведени€ двух векторов ( skalar(Vector vector1, Vector vector2) ). 


¬ классе Main приведены примеры всех, описанных выже операций.